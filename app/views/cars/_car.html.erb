<div id="<%= dom_id car %>" class="card m-2">
    <div class="card-body">
        <strong><%= car.licence_plate %></strong>
        <div class="form-actions">
            <%= button_to "Borrar", car, method: :delete, class:"btn btn-danger"%>
        </div>
    </div>
</div>
