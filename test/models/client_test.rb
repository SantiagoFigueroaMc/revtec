# frozen_string_literal: true

require 'test_helper'

class ClientTest < ActiveSupport::TestCase
  # test "the truth" do
  #   assert true
  # # end
  # test "should not save client without name" do
  #   client = Client.new
  #   assert_not client.save, "Saved the client without a name"
  # end
  # test "should save client without address" do
  #   client = Client.new
  #   client.name = "John Doe"
  #   assert client.save, "Did not save the client without an address"
  # end
  # test "should save client without phone" do
  #   client = Client.new
  #   client.name = "John Doe"
  #   assert client.save, "Did not save the client without a phone"
  # end
end
